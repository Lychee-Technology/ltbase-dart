name: ltbase_client
description: A command line tool for testing LTBase Notes API
version: 1.0.0

environment:
  sdk: ^3.0.0

dependencies:
  args: ^2.4.2        # Official command line argument parser
  crypto: ^3.0.3      # SHA256/SHA512 hashing utilities

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
